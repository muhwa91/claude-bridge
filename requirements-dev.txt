# claude-bridge 개발 의존성(품질 게이트 도구). 구동에는 필요 없다 — 런타임은 requirements.txt.
#
# 설치:
#   python -m pip install -r requirements-dev.txt
#
# 왜 따로 선언하나: 2026-07-29 노트북에서 mypy 만 없어 타입체크가 조용히 건너뛰어졌다.
# 세 도구 모두 어디에도 선언돼 있지 않아, 깔려 있던 ruff·pytest 는 우연이었다. 선언이 없으면
# 새 머신은 게이트가 반쪽만 도는 채로 "세팅 완료"가 된다.
#
# 버전 고정: 린터·타입체커는 버전이 오르면 없던 지적이 새로 나와 게이트가 깨진다.
# 올릴 때는 의도적으로 올리고 지적을 정리한다(런타임 requirements.txt 의 핀 원칙과 동일).
mypy==2.3.0
ruff==0.15.21
pytest==9.1.1
